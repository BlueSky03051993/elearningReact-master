import { FETCH_ROLE_LIST } from './roleConstants';
import { OPEN_NAVBAR, FETCH_SELECTED, CHANGE_TABLE } from './adminConstants';
import { TOKEN_KEY, SET_TOKEN } from './loginConstants'

const Constants = {
    FETCH_ROLE_LIST,
    OPEN_NAVBAR,
    CHANGE_TABLE,
    FETCH_SELECTED,
    DOMAIN_API: 'http://localhost:8080',
    TOKEN_KEY,
    SET_TOKEN,
};

export default Constants;